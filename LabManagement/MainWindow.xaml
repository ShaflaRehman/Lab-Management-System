<Window x:Class="LabManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" 
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A" FontFamily="Segoe UI">

    <Grid>
        <Border Width="400" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>

                <!-- Username -->
                <Border CornerRadius="6" Background="#1E293B" BorderBrush="#3B82F6" BorderThickness="1" Height="40" Margin="0,10,0,5">
                    <TextBox Name="txtUsername" Background="Transparent" BorderThickness="0" Foreground="#93C5FD"
                             FontSize="14" Padding="10" Text="Username" FontStyle="Italic" Opacity="0.7"
                             GotFocus="txtUsername_GotFocus" LostFocus="txtUsername_LostFocus"/>
                </Border>

                <!-- Password -->
                <Border CornerRadius="6" Background="#1E293B" BorderBrush="#3B82F6" BorderThickness="1" Height="40" Margin="0,10,0,5">
                    <Grid>
                        <PasswordBox Name="txtPassword" Background="Transparent" Foreground="#93C5FD"
                                     BorderThickness="0" FontSize="14" Padding="10" 
                                     GotFocus="txtPassword_GotFocus" LostFocus="txtPassword_LostFocus"
                                     PasswordChanged="txtPassword_PasswordChanged"/>
                        <TextBlock x:Name="passwordPlaceholder" Text="Password" Foreground="#93C5FD" Opacity="0.7" 
                                   Margin="10,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False"
                                   FontSize="14" FontStyle="Italic"/>
                    </Grid>
                </Border>

                <!-- Role -->
                <TextBlock Text="Role:" Foreground="White" Margin="0,10,0,5"/>
                <ComboBox x:Name="cbRole" 
                          Background="Transparent"
                          Foreground="#93C5FD"
                          FontSize="14"
                          FontStyle="Italic"
                          BorderThickness="0"
                          Padding="10"
                          Opacity="0.85">
                    <ComboBoxItem Content="Student"/>
                    <ComboBoxItem Content="Faculty"/>
                    <ComboBoxItem Content="HOD"/>
                    <ComboBoxItem Content="Lab Attendant"/>
                </ComboBox>

                <!-- Login Button -->
                <Button Content="LOGIN" Height="40" Background="#4ADE80" Foreground="Black" FontWeight="Bold"
                        BorderThickness="0" Margin="0,20,0,10" Cursor="Hand" Click="Login_Click" />

                <!-- Sign up -->
                <TextBlock Text="Don't have an account? Sign up"
                           Foreground="#38BDF8" TextDecorations="Underline" FontStyle="Italic"
                           MouseDown="SignUp_Click"
                           Cursor="Hand"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
